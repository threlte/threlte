/**
 * This is the type of the props that are passed to the <T> component by the
 * vite plugin.
 */
export type StudioProps = {
  moduleId: string
  index: number
  signature: string
  pathItems?: string[]
}
