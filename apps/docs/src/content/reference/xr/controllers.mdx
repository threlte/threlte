---
{
  category: '@threlte/xr',
  name: '<Controllers>',
  order: 2,
  type: 'component',
  componentSignature:
    {
      props:
        [
          {
            name: 'modelLeft',
            type: "THREE.Object3D | 'none' | undefined",
            required: false,
            description: 'An optional model for the left controller. If "none" is passed, no controller model will render.'
          },
          {
            name: 'modelRight',
            type: "THREE.Object3D | 'none' | undefined",
            required: false,
            description: 'An optional model for the right controller. If "none" is passed, no controller model will render.'
          },
        ],
      events:
        [
          {
            name: 'connected',
            payload: "XRControllerEvent<'connected'>",
            description: 'Fired when a controller connects.'
          },
          {
            name: 'disconnected',
            payload: "XRControllerEvent<'disconnected'>",
            description: 'Fired when a controller disconnects.'
          },
          {
            name: 'select',
            payload: "XRControllerEvent<'select'>",
            description: 'Fired when a the user has completed a primary action.'
          },
          {
            name: 'selectstart',
            payload: "XRControllerEvent<'selectstart'>",
            description: 'Fired when a the user begins a primary action.'
          },
          {
            name: 'selectend',
            payload: "XRControllerEvent<'selectend'>",
            description: 'Fired when a the user ends a primary action or when a controller that is in the process of handling an ongoing primary action is disconnected without successfully completing the action.'
          },
          {
            name: 'squeeze',
            payload: "XRControllerEvent<'squeeze'>",
            description: 'Fired when a controller has completed a primary squeeze action.'
          },
          {
            name: 'squeezestart',
            payload: "XRControllerEvent<'squeezestart'>",
            description: 'Fired when the user begins a primary squeeze action on one of its input sources.'
          },
          {
            name: 'squeezeend',
            payload: "XRControllerEvent<'squeezeend'>",
            description: 'Fired when a the user ends a primary squeeze action or when a controller that is in the process of handling an ongoing primary squeeeze action is disconnected without successfully completing the action.'
          },
        ],
    }
}
---

`<Controllers />` represents a THREE.XRTargetRaySpace, a THREE.XRGripSpace, and a controller model per each hand.

You can specify custom models, or no models to be rendered for these controllers.

This component can accept children in `left` and `right` slots, which will be parented to the controller of the respective handedness.

```svelte
<Controllers>
  <T slot='left' is={$gltf1.scene} />
  <T slot='right' is={$gltf2.scene} />
</Controllers>
```
